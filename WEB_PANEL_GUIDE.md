# Web管理面板使用指南

## 快速开始

### 1. 启动管理面板

```bash
# Windows
start_web_panel.bat

# 或直接运行
python src/web/app.py
```

启动后，在浏览器中访问：http://127.0.0.1:5000

## 功能说明

### AI配置

**基本设置**
- **AI提供商**：选择豆包、Claude或OpenAI
- **模型/Endpoint ID**：
  - 豆包：填写Endpoint ID（如：ep-20251226192412-8kff2）
  - Claude：填写模型名称（如：claude-3-5-sonnet-20241022）
  - OpenAI：填写模型名称（如：gpt-4）
- **Temperature**：控制回复的随机性（0=保守，1=创造性）
- **最大Token数**：单次回复的最大长度
- **上下文消息数**：保留的历史消息数量

**API密钥配置**
- 填写对应AI提供商的API密钥
- 密钥会安全保存到 `.env` 文件
- 已设置的密钥会显示为"已设置（点击修改）"

**测试连接**
- 点击"测试连接"按钮验证API配置是否正确
- 测试成功会显示连接状态和延迟

### 人格配置

**基本信息**
- 姓名、年龄、性别、职业

**性格特征**
- 性格特点：每行输入一个特点（如：温柔体贴、善解人意）
- 兴趣爱好：每行输入一个爱好（如：看电影、听音乐）

**说话风格**
- 语气：描述整体语气（如：温柔亲切）
- 语气词：用逗号分隔（如：呀,哦,呢,啦）
- 常用表达：用逗号分隔（如：嘻嘻,哈哈,嗯嗯）

**关系设定**
- 角色：女朋友/朋友/助手
- 亲密程度：亲密/友好/正式
- 行为特征：每行输入一个行为（如：关心对方的日常）

### 微信配置

**基本设置**
- **微信路径**：微信安装目录（不是exe文件）
  - 示例：`C:\Program Files\Tencent\Weixin`
- **自动回复**：开关自动回复功能
- **回复延迟**：设置最小和最大延迟时间（秒）
- **最大消息长度**：限制单条消息的最大字符数

**过滤器设置**
- **白名单**：每行一个微信昵称，只回复白名单中的用户（为空则接受所有）
- **黑名单**：每行一个微信昵称，不回复黑名单中的用户
- **关键词过滤**：每行一个关键词，包含这些关键词的消息不回复

## 使用流程

### 首次配置

1. **配置AI**
   - 选择AI提供商（推荐豆包）
   - 填写模型ID和API密钥
   - 点击"测试连接"验证
   - 点击"保存配置"

2. **配置人格**
   - 填写基本信息
   - 设置性格特征和兴趣爱好
   - 自定义说话风格
   - 点击"保存配置"

3. **配置微信**
   - 填写微信路径
   - 设置回复参数
   - 配置过滤器（可选）
   - 点击"保存配置"

4. **启动机器人**
   - 关闭管理面板
   - 运行 `python src/main.py`

### 修改配置

1. 启动管理面板
2. 切换到对应的配置页面
3. 修改需要更改的参数
4. 点击"保存配置"
5. 重启机器人使配置生效

## 注意事项

1. **配置保存**：所有配置会保存到 `config/` 目录下的文件中
2. **API密钥安全**：密钥保存在 `.env` 文件中，不要将此文件提交到Git
3. **配置生效**：修改配置后需要重启主程序才能生效
4. **端口占用**：管理面板使用5000端口，确保端口未被占用
5. **浏览器兼容**：推荐使用Chrome、Edge或Firefox浏览器

## 常见问题

**Q: 保存配置后没有生效？**
A: 需要重启主程序（`python src/main.py`）才能使新配置生效。

**Q: 无法访问管理面板？**
A:
- 检查Flask是否已安装：`pip install flask flask-cors`
- 确认5000端口未被占用
- 查看启动日志中的错误信息

**Q: API密钥显示为"已设置"，如何查看？**
A: 出于安全考虑，已设置的密钥不会在界面显示。如需修改，直接输入新密钥即可。

**Q: 如何备份配置？**
A: 备份 `config/` 目录下的所有文件即可。

## 技术支持

如有问题，请查看：
- 项目README：`README.md`
- 日志文件：`logs/ailive.log`
- 提交Issue：GitHub Issues
